<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nahiduddinahammed.minidoctorproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MainPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <activity android:name=".SliderViewActivity" />
        <activity android:name=".UserRegistrationActivity" />
        <activity android:name=".LoginPageActivity" />
        <activity android:name=".DoctorsPageActivity" />
        <activity android:name=".FirstaidActivity" />
        <activity android:name=".HospitalActivity" />
        <activity android:name=".USSDActivity" />
        <activity android:name=".EmbulenceActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".BMIActivity" />
        <activity android:name=".FavouriteActivity" />
        <activity android:name=".ConsultantListView" />
        <activity android:name=".FirstAidItemListView"/>
        <activity android:name=".MedicinePageActivity">

        </activity>
    </application>

</manifest>